describe('Create user query tests', () => {
  test('should throw validation error if name not have spaces', () => {});
  test('should throw validation error if age is less than 18 years', () => {});
  test('should throw error if cpf not have the correct value of chars', () => {});
});
